# Copy file này thành .env và điền giá trị thực

# Local dev với SQLite:
# DATABASE_URL="file:./prisma/dev.db"

# Production với PostgreSQL (Railway):
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
